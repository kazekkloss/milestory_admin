import 'package:flutter/material.dart';

class RoadManagementPage extends StatelessWidget {
  const RoadManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ROADS MENAGEMENT PAGE", style: TextStyle(color: Colors.white),),
      ),
    );
  }
}